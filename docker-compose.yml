version: '3.8'
services:
  mes:
    build: .
    ports:
      - "5001:5001"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - PORT=5001
    volumes:
      - mes_data:/app/database
    restart: unless-stopped
volumes:
  mes_data:
